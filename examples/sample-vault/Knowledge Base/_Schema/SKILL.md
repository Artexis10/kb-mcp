---
name: knowledge-base
description: Minimal public sample schema for kb-mcp smoke tests.
version: 0.1.0
---

# Knowledge Base Sample Schema

This is a deliberately tiny schema stub for the public sample vault. The full
starter schema ships in `src/kb_mcp/_scaffold/_Schema/` and is installed by
`kb-mcp init`.
