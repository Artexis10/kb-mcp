---
tags: [strategic-thinking, curated]
---

# Sample Curated Note

This file lives outside Knowledge Base/. It's used by tests to verify
that `find(scope="vault")` reaches sibling (read-only) folders and
`find(scope="kb")` does not.

Marker token for grep: reference-marker-xyz
