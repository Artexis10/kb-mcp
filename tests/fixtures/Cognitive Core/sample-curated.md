---
tags: [strategic-thinking, curated]
---

# Sample Curated Note

This file lives outside Knowledge Base/. It's used by tests to verify
that `find(scope="vault")` reaches curated trees and `find(scope="kb")`
does not.

Marker token for grep: cognitive-core-marker-xyz
